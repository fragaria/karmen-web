import React from "react"

const TestimonialsItem = ({ resources, site, location, ...props }) => {
  const {frontmatter,html} = resources;

  return (
    <article
      className={"testimonials__item"}
    >
      testimonials__item
    </article>
  )
}

export default TestimonialsItem
